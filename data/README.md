# Local data

The analysis expects semicolon-delimited, preprocessed participant CSVs in:

```text
data/HUPA-UCM Diabetes Dataset/Preprocessed/
```

The original data remains local and is excluded from Git. Do not add participant-level or raw device data without confirming the dataset’s license, consent terms, and data-sharing requirements.
